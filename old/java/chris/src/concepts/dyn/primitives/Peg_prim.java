package concepts.dyn.primitives;

import concepts.dyn.Primitive;

/**
 * Not a string, not a set, not a list, just nothing. Like a Peg_prem, but without activation.
 * Was created for use of the PrimusInterPares_prem as a member.
 * @author su
 */
public class Peg_prim extends Primitive {

    /** Constructor. */
    public Peg_prim() {
    }
}   // class
